# Core
torch
torchvision
pytorch-lightning
numpy
scipy
einops
timm
omegaconf
boto3           # imported by src/model_utils.py as soon as the model code loads

# 3D
open3d          # no PyPI wheel for Python 3.13: install Open3D's development build (see README)
trimesh
pymcubes
scikit-image
pytorch-wavelets
PyWavelets
h5py

# Sparse convolutions used by WaLa's encoder. Pick the build matching torch.version.cuda:
#   CUDA 11.8 -> spconv-cu118   12.1-12.3 -> spconv-cu121   12.4-12.5 -> spconv-cu124   12.6+ -> spconv-cu126
#   All four ship wheels for Python 3.9 to 3.13. spconv-cu120 stops at 3.11 -- avoid it.
#   Apple Silicon: build spconv natively; see README "Environment".
spconv>=2.3.6; sys_platform == 'darwin'
spconv-cu124; sys_platform == 'linux'

# Image conditioning
Pillow
transformers
open-clip-torch==2.24.0
diffusers
huggingface_hub
ftfy
regex
fasteners
setuptools<82   # pytorch_wavelets imports pkg_resources, removed in setuptools 82.0.0

# DINOv2 is fetched through torch.hub at first run.
